---

#
# variables needed to be defined in user's playbook
#



#
# user-configurable defaults
#

nodejs_compile: True   # install tools for compiling native addons from npm


nodejs_default_in_apt: "4.0"
nodejs_default_in_yum: "0.10"
